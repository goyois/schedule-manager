# 성능 개선 기록 (email 인덱스 + 리포트 쿼리 범위 축소 + 벌크 INSERT 배치화)

> 2026-08-03. AI/캐시를 뺀 나머지 영역에서 성능 개선 후보를 감사(audit)해 찾은 세 가지를 순서대로
> 고쳤다. 세 개 모두 코드에서 실제로 확인한 문제이고, 각각 As-Is/To-Be를 직접 측정했다.

## 1. `User.email`에 인덱스가 없었음 — 인증 경로의 가장 뜨거운 조회

### As-Is

`User` 엔티티(`domain/user/entity/User.java`)의 `email` 컬럼에 인덱스나 유니크 제약이 없었다.
그런데 `UserRepository.findByEmail`은 인증된 **모든** 요청마다 `JwtAuthenticationFilter` →
`CustomUserDetailsService.loadUserByUsername`을 거쳐 호출되고, 각 서비스(`ScheduleService`,
`CategoryService`, `AiService`, `ReportService`, `MandalartService`, `UserService` 등)의
`findUserByEmail` 헬퍼가 `requesterEmail`을 `User`로 다시 resolve하려고 **요청당 한 번 더** 같은
조회를 한다 — 요청 하나에 인덱스 없는 `email` 조회가 최소 2번.

### 실측 (`UserEmailIndexPerformanceTest`, users 5만 행 시딩)

| | 실행계획 | cost | 평균 응답시간 |
|---|---|---:|---:|
| AS-IS | `Seq Scan on users` | 0.00..1323.61 | 3.45ms |
| TO-BE | `Index Scan using idx_user_email` | 0.39..32.66 | 0.05ms |

### To-Be

`@Table(indexes = @Index(name = "idx_user_email", columnList = "email", unique = true))`를
`User` 엔티티에 추가하고, 로컬 DB에 `CREATE UNIQUE INDEX idx_user_email ON users(email);`을
적용했다. 유니크로 잡은 이유는 `email`이 로그인 식별자라 애초에 유일해야 하고(`UserService.
signup`이 `existsByEmail`로 이미 애플리케이션 레벨에서 검사하고 있었음 — 인덱스는 그 위에 DB
레벨 백스톱을 추가한 것), 적용 전 로컬 DB에 중복 이메일이 없는 것도 미리 확인했다.

## 2. `ReportService`가 리포트 하나 뽑을 때도 유저 전체 이력을 무제한 조회

### As-Is

`ReportService.getStats`/`getInsight`/`getCachedInsight`가 전부 `scheduleService.getSchedules
(..., null)`(범위 없음)로 유저가 지금까지 만든 **모든** 일정을 가져온 뒤, `computeStats`/
`overlapping()`으로 메모리에서 이번 기간·직전 기간만 걸러냈다. "이번 주" 리포트 하나 보려고 해도
비용이 유저의 평생 일정 건수에 비례해서 커지는 구조였다 — 일정이 쌓일수록(반복 일정이 계속
쌓이는 유저는 특히) 매 리포트 요청마다 더 느려진다.

### To-Be

`ScheduleRepositoryCustom`에 `searchSchedulesInRange`(status 필터/페이징 없이 `[rangeStart,
rangeEnd)` 범위만 DB에서 좁혀 조회 — 보드 뷰가 이미 쓰던 `overlapsRange` QueryDSL 조건 재사용)를
추가했다. `getStats`/`getInsight`는 이번 기간(`range`)과 직전 기간(`previousRange`)이 항상 붙어
있다는 점을 이용해 `[prevRange[0], range[1]]` 하나의 bounded 조회로 두 기간을 한 번에 커버하고,
`computeStats`/`overlapping` 내부 로직은 그대로 둔 채 입력 리스트만 좁혔다. `getCachedInsight`는
직전 기간이 필요 없어 `[range[0], range[1])`만 조회한다.

다만 `buildRagContext`(RAG로 찾은 "다른 시기의 비슷한 활동")는 매칭된 일정이 이번/직전 기간
밖(몇 달 전 등)일 수 있어 이 bounded 조회로는 못 잡는다 — 예전엔 이걸 피하려고 전체 이력을 항상
미리 들고 있었는데, 대신 `searchSchedulesByIds`(RAG_TOP_K=5개 이하 id를 직접 조회)로 targeted
조회하는 쪽으로 바꿨다. 매칭이 실제로 있을 때만(그것도 최대 5건) 발생하는 이 작은 조회가, 매
리포트 요청마다 무제한 전체 조회를 하는 것보다 훨씬 싸다.

### 실측 (`ReportQueryPerformanceTest`, 한 유저가 3년치 매일 일정 1,095건을 가진 상황에서 "이번 주"
리포트에 필요한 범위만 비교)

| | 반환 건수 | 평균 응답시간 |
|---|---:|---:|
| AS-IS (`searchSchedules`, 범위 없음) | 1,095 | 3.85ms |
| TO-BE (`searchSchedulesInRange`, 21일 범위) | 21 | 0.75ms |

이 격차는 유저의 누적 일정이 늘어날수록(반복 일정이 몇 년 쌓인 유저 등) AS-IS 쪽만 계속
나빠지는 구조라, 로컬 3년치 시딩보다 실제 운영에서 격차가 더 벌어질 가능성이 높다.

## 3. `GenerationType.IDENTITY`가 벌크 INSERT의 JDBC 배치를 막고 있었음

### As-Is

`Schedule`/`MandalartCell` 모두 `@GeneratedValue(strategy = GenerationType.IDENTITY)`였다.
IDENTITY 전략은 Hibernate가 생성된 키를 행마다 즉시 읽어와야 해서 JDBC 배치 INSERT가 근본적으로
안 된다 — `RecurringScheduleService.materializeOccurrences`가 반복 규칙 하나당 수십 건을
`ScheduleService.createSchedules`(`scheduleRepository.saveAll`)로 "한 번에" 저장해도, 실제로는
건별 개별 INSERT 왕복이었다. `MandalartService.createBoard`가 보드 하나당 81개 셀을 `saveAll`로
저장하는 것도 마찬가지.

### To-Be

`Schedule`/`MandalartCell`을 `GenerationType.SEQUENCE`(`allocationSize = 50`)로 바꾸고,
`application.yml`에 `hibernate.jdbc.batch_size: 50` + `order_inserts`/`order_updates: true`를
추가했다. SEQUENCE 전략은 Hibernate가 시퀀스에서 미리 50개씩 id를 client-side로 받아와, 실제
INSERT는 JDBC 배치로 한 번에 내보낼 수 있다.

**시행착오**: 처음엔 기존 IDENTITY 컬럼이 내부적으로 쓰던 시퀀스(`schedules_schedule_id_seq`
등)를 그대로 SEQUENCE 전략이 가리키게 하려 했다(별도 시퀀스를 새로 안 만들면 id 충돌 걱정이
없으니까). 그런데 `ddl-auto: validate`가 즉시 `SchemaManagementException: Schema-validation:
missing sequence`로 실패했다 — Postgres의 `information_schema.sequences`는 IDENTITY 컬럼이
소유한 시퀀스를 스펙상 의도적으로 제외하는데(`pg_depend.deptype = 'i'`인 것들을 필터링),
Hibernate의 스키마 검증이 정확히 그 뷰로 시퀀스 존재를 확인하기 때문이다(`pg_sequences`에는
보이는데 `information_schema.sequences`에는 없어서 처음엔 원인을 착각하기 쉬웠다). 그래서 별도
이름(`schedule_id_seq_v2`, `mandalart_cell_id_seq_v2`)의 새 시퀀스를 만들고, 기존 IDENTITY
시퀀스의 마지막 값보다 충분히 위(1000만)에서 시작하게 해 충돌 여지를 없앴다 — 컬럼 자체는 여전히
`GENERATED BY DEFAULT AS IDENTITY`라 값이 없을 때만 옛 시퀀스를 쓰는데, 이제 Hibernate가 항상
명시적으로 값을 채우므로 그 경로는 사실상 죽어있다.

### 실측 (`ScheduleBulkInsertPerformanceTest`, `saveAll`로 1,000건 저장)

| | 소요 시간 | 건당 평균 |
|---|---:|---:|
| AS-IS (IDENTITY) | 241ms | 0.241ms |
| TO-BE (SEQUENCE + batch_size=50) | 147ms | 0.147ms |

39% 단축. 실제로 배치가 걸렸는지는 시퀀스 값으로 직접 확인했다 — `nextval()`을 1,000번이 아니라
약 19~20번만 호출한 흔적(시퀀스가 정확히 50의 배수만큼 전진)으로 client-side pooled 할당이
작동하는 걸 확인했다. 로컬은 DB가 loopback이라 왕복 지연 자체가 이미 작아서(건당 0.24ms) 개선폭이
상대적으로 작게 보이지만, 이 프로젝트의 실제 배포 대상인 RDS(네트워크 너머, 왕복 지연이 훨씬 큼)
에서는 왕복 횟수를 줄이는 이 최적화의 체감 효과가 로컬보다 더 클 것으로 예상한다(별도로 RDS에서
재측정하지는 않았다 — 로컬 환경 한계로 명시해둔다).

### 운영(RDS) 반영 시 필요한 작업 (아직 안 함)

이 저장소는 Flyway/Liquibase가 없어 스키마를 수동 DDL로 관리한다(`AGENTS.md`). 로컬엔 이미
적용했지만, RDS에도 다음을 똑같이 적용해야 한다:
```sql
CREATE SEQUENCE schedule_id_seq_v2 INCREMENT BY 50 START WITH <RDS의 schedules_schedule_id_seq 마지막 값 + 충분한 여유값>;
CREATE SEQUENCE mandalart_cell_id_seq_v2 INCREMENT BY 50 START WITH <RDS의 mandalart_cells_mandalart_cell_id_seq 마지막 값 + 충분한 여유값>;
```
시작값은 반드시 RDS의 실제 시퀀스 마지막 값(`SELECT last_value FROM schedules_schedule_id_seq;`)을
배포 직전에 다시 조회해서 계산해야 한다 — 로컬과 운영의 누적 행 수가 다르므로 이 문서의 숫자를
그대로 쓰면 안 된다.

## 참고

- 코드: `User`(인덱스), `ScheduleRepositoryCustom`/`ScheduleRepositoryImpl`/`ScheduleService`/
  `ReportService`(bounded 쿼리), `Schedule`/`MandalartCell`/`application.yml`(시퀀스 배치)
- 테스트: `UserEmailIndexPerformanceTest`, `ReportServiceTest`, `ReportQueryPerformanceTest`,
  `ScheduleBulkInsertPerformanceTest`
- 관련 문서: RAG 파이프라인 개선은 [`RAG_IMPROVEMENTS.md`](./RAG_IMPROVEMENTS.md) 참고
- 다음 후보(아직 미착수): `schedules` 캐시 evict를 SCAN에서 유저별 버전 카운터로 바꿔 O(1)
  무효화로 전환하는 것.
